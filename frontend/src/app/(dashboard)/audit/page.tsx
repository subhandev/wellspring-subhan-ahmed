export default function AuditPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Audit log</h1>
      <p className="mt-2 text-muted-foreground">Scaffold — filters by date range and action type.</p>
    </div>
  );
}
