export default function ImportPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">CSV import</h1>
      <p className="mt-2 text-muted-foreground">Scaffold — bulk sessions import + row-level errors.</p>
    </div>
  );
}
