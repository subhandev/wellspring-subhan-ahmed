# API origin only, no trailing slash (client uses /v1/... paths)
NEXT_PUBLIC_API_URL="http://localhost:4000"
# Production (Railway): NEXT_PUBLIC_API_URL="https://wellspring-subhan-ahmed-production.up.railway.app"
