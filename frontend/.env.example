NEXT_PUBLIC_API_URL="http://localhost:4000"

# API routes are versioned under /v1 on the backend (e.g. http://localhost:4000/v1/programs).
# Set NEXT_PUBLIC_API_URL to the API origin only (no trailing slash).
