# Cursor agent — remove S3 relay (2026-05-13)

Raw notes for the change that removed `POST /v1/uploads/relay` end-to-end (API, frontend fallback, relay tests, `media.relay_uploaded` audit enum + migration, docs/README/CORS).
